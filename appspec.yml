version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/website #where to store the artifacts
hooks:
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: ec2-user
